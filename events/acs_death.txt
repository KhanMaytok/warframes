namespace = acs_death

acs_death.0001 = {
	type = character_event
	hidden = yes

	trigger = {
		has_character_flag = flag_warframe_carrier
	}

	immediate = {
		random_province = {
			save_scope_as = warframe_location
			add_province_modifier = {
				modifier = warframe_province_modifier
				days = 3650
			}
			province_owner = {
				add_trait = intellect_good_3
				random_courtier = {
					limit = {
						is_female = no
					}
					add_trait = rhino_warframe
				}
			}
		}
	}
}
